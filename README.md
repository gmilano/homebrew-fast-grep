# homebrew-fast-grep

Homebrew tap for [fast-grep](https://github.com/gmilano/fast-grep-rust).

```bash
brew install gmilano/fast-grep/fast-grep
```

The formula in `Formula/fast-grep.rb` is regenerated after each upstream
release by `packaging/homebrew/update-formula.sh` in the main repo.
